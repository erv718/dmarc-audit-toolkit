# CLAUDE.md

Read **`AGENTS.md`** - it is the canonical agent contract for this repo and is
kept tool-neutral so any coding agent can load it. This file exists so Claude
Code (and the external links that point here) land in the right place.

Everything in AGENTS.md applies verbatim: the read-only ground rules, the
credential-handling rule, the verification habits that prevent the classic
wrong conclusions, the export data dictionary, and the working sequence in
`docs/methodology.md`.
